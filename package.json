{
  "name": "erp-control-center",
  "version": "1.0.0",
  "description": "ERP Control Center - shared multi-user dashboard (SAP Master Data, Inventory, Projects) with a lightweight JSON-file backend, no native dependencies.",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js"
  },
  "license": "ISC",
  "dependencies": {
    "express": "^4.22.2"
  }
}
